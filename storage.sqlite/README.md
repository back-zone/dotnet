#### back.zone's

### SQLITE

#### Configuration object

```json
{
  "sqlite": {
    "db_path": [
      "back.zone.db"
    ],
    "min_pool_size": 5,
    "max_pool_size": 20,
    "password": "optional-password"
  }
}
```
